---
---

Converts drilldown's remaining `.test.ts` suites to the loop kit and stops the test runner globbing that pattern. Tests, changelog stubs, and docs only — no package behaviour changes, so no version bump.
