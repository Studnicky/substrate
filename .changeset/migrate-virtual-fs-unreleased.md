---
"@studnicky/virtual-fs": major
---

`EntryEntity.Type` is the schema-derived filesystem entry data. `VirtualFileSystemOptionsInterface`, `FileSystemInterface`, and `StatResultInterface` define runtime and readonly contracts. `@studnicky/virtual-fs` is the sole public code entrypoint.
