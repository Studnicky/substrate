/** Static, read-only input data for examples/patch-datatype.ts. */
export namespace PatchDatatypeFixture {
  export const M1 = new Map([['a', 1]]);

  export const M2 = new Map([['a', 1]]);

  export const Tree = { 'root': { 'child': { 'value': 42 } } };
}
