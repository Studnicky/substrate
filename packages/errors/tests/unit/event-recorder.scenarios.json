{
  "cases": [
    {
      "name": "detaches-recorded-events",
      "description": "detaches recorded events and every public projection",
      "shape": "detaches-recorded-events",
      "input": { "recorder": { "event": { "shape": "request", "nested": { "value": 1 } } } },
      "expected": { "firstProjection": { "shape": "request", "nested": { "value": 1 } }, "detachedProjection": { "shape": "request", "nested": { "value": 1 } } }
    }
  ]
}
