{
  "cases": [
    {
      "name": "detaches-recorded-events",
      "description": "detaches recorded events and every public projection",
      "kind": "detaches-recorded-events",
      "input": { "recorder": { "event": { "kind": "request", "nested": { "value": 1 } } } },
      "expected": { "firstProjection": { "kind": "request", "nested": { "value": 1 } }, "detachedProjection": { "kind": "request", "nested": { "value": 1 } } }
    }
  ]
}
