{
  "cases": [
    {
      "description": "FileLockConfigError carries the invalid-config code and provided message",
      "expected": { "code": "fileLock.invalidConfig", "message": "invalid" },
      "input": { "message": "invalid" },
      "shape": "constructs-with-code",
      "name": "constructs-with-code"
    }
  ]
}
