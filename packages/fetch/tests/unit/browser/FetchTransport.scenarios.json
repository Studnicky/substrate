{
  "cases": [
    {
      "name": "uses-native-fetch",
      "description": "FetchTransport uses native fetch when no dispatcher is supplied",
      "operation": "uses-native-fetch",
      "input": { "input": "https://example.com/resource", "init": { "method": "GET" } },
      "expected": {
        "input": "https://example.com/resource",
        "init": {
          "method": "GET"
        }
      }
    },
    {
      "name": "rejects-node-dispatcher",
      "description": "FetchTransport rejects Node dispatchers without calling native fetch",
      "operation": "rejects-node-dispatcher",
      "input": { "message": "undici connection pooling requires a Node.js runtime; the browser uses native fetch" },
      "expected": {
        "message": "undici connection pooling requires a Node.js runtime; the browser uses native fetch"
      }
    }
  ]
}
