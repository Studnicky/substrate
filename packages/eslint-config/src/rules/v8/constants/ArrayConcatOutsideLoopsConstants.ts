/** Data constants for the `array-concat-outside-loops` rule: the rule name and its violation message. */

export const RULE_NAME = 'v8Optimization/arrayConcatOutsideLoops';
export const MESSAGE = 'Avoid concat() in loops. It creates new arrays each iteration.';
