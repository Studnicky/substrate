/** Data constants for the `type-alias-invariants` rule: the matchers used to locate and safely remove a `readonly` modifier token during autofix. */

export const READONLY_KEYWORD_PATTERN = /\breadonly\b/u;

export const TRAILING_PLUS_PATTERN = /\+\s*$/u;
