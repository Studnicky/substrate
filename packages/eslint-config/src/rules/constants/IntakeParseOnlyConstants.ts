/** Data constants for the `intake-parse-only` rule: the default exempt package names and the entity member that accepts unparsed input. */

export const DEFAULT_EXEMPT_PACKAGES = ['@studnicky/types', '@studnicky/eslint-config'];

export const INTAKE_MEMBER = 'intake';
