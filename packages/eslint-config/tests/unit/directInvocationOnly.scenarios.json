{
  "valid": [
    { "code": "class P { apply(x: number): void { void x; } } const p = new P(); p.apply(1);", "name": "class instance with apply method — not a Function, no report" },
    { "code": "class Q { call(x: number): void { void x; } } const q = new Q(); q.call(2);", "name": "class instance with call method — not a Function, no report" },
    { "code": "class R { bind(x: number): void { void x; } } const r = new R(); r.bind(3);", "name": "class instance with bind method — not a Function, no report" },
    { "code": "const o = { apply(n: number): void { void n; } }; o.apply(4);", "name": "plain object with apply method — not callable, no report" },
    { "code": "const a: any = {}; a.apply(null);", "name": "any-typed receiver — unprovable, no report" },
    { "code": "declare const obj: { run(): void }; obj.run();", "name": "normal non-bind method call — no report" },
    { "code": "class P { apply(x: number): void { void x; } } const p = new P(); const alias = p.apply; alias(1);", "name": "aliasing a class instance's apply method — not a Function, no report" },
    { "code": "declare const obj: { run(): void }; (0, obj.run)();", "name": "comma-sequence-expression callee on a non-banned method — no report" }
  ],
  "invalid": [
    { "code": "function f(x: number, y: number): void { void x; void y; } f.apply(null, [1, 2]);", "errors": [{ "messageId": "forbidden" }], "name": "function declaration .apply — forbidden" },
    { "code": "const g = (): void => {}; g.bind(null);", "errors": [{ "messageId": "forbidden" }], "name": "arrow function .bind — forbidden" },
    { "code": "const h = function (): void {}; h.call(undefined);", "errors": [{ "messageId": "forbidden" }], "name": "function expression .call — forbidden" },
    { "code": "function k(): void {} k['apply'](null, []);", "errors": [{ "messageId": "forbidden" }], "name": "computed k['apply'] on function declaration — forbidden" },
    { "code": "function f(x: number): void { void x; } const rebind = f.bind; rebind(null);", "errors": [{ "messageId": "forbidden" }], "name": "aliasing .bind to a variable then calling the alias — forbidden" },
    { "code": "function f(x: number): void { void x; } (0, f.bind)(null);", "errors": [{ "messageId": "forbidden" }], "name": "comma-sequence-expression callee (0, f.bind)(null) — forbidden" }
  ]
}
