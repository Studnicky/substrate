{
  "cases": [
    {
      "name": "delay-resolves-without-signal",
      "description": "Delay resolves after the timeout elapses when no signal is provided",
      "shape": "delay-resolves-without-signal",
      "input": { "timeoutMs": 1 },
      "expected": { "resolved": true }
    },
    {
      "name": "delay-resolves-with-never-aborted-signal",
      "description": "Delay resolves after the timeout elapses when the signal is never aborted",
      "shape": "delay-resolves-with-never-aborted-signal",
      "input": { "timeoutMs": 1 },
      "expected": { "resolved": true }
    },
    {
      "name": "delay-rejects-already-aborted",
      "description": "Delay rejects with ThrottleAbortedError when the signal is already aborted",
      "shape": "delay-rejects-already-aborted",
      "input": { "timeoutMs": 50 },
      "expected": { "errorCode": "throttle.aborted", "errorMessage": "The operation was aborted", "timeoutMs": 50 }
    },
    {
      "name": "delay-rejects-before-timeout",
      "description": "Delay rejects with ThrottleAbortedError when the signal aborts before the timeout",
      "shape": "delay-rejects-before-timeout",
      "input": { "timeoutMs": 50 },
      "expected": { "errorCode": "throttle.aborted", "errorMessage": "The operation was aborted", "timeoutMs": 50 }
    },
    {
      "name": "delay-removes-abort-listener",
      "description": "Delay removes the abort listener once the timer resolves normally",
      "shape": "delay-removes-abort-listener",
      "input": { "timeoutMs": 1 },
      "expected": { "abortListenerAddCount": 1, "abortListenerRemoveCount": 1 }
    }
  ]
}
